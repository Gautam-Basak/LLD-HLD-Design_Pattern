package com.example.designpattern.ProxyDesignPattern.EmployeeExample;

public class Info {
    
    // This example is like security.
    // Before it actually interect with DB directly it will first pass to the proxy
    // where it actually checks whether the client have required permission to create,
    // delete or update or fetch etc.
}
