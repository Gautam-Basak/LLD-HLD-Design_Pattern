package com.example.designpattern.ProxyDesignPattern.ImageExample;

public class Info {
    
    // This ia an example of Caching. Unless it a first time or new request we will
    // not hit the real object instead we will get the work done by using proxy.
}
